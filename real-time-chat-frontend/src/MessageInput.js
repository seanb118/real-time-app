import React from 'react';

const MessageInput = () => {
    return (
        <div className="message-input">
            {/* Input field for typing messages with emoji support */}
        </div>
    );
};

export default MessageInput;