import React from 'react';

const ContactList = () => {
    return (
        <div className="contact-list">
            {/* Display list of contacts with profile pictures and names */}
        </div>
    );
};

export default ContactList;